#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, q;
    cin >> n >> q;

    while (q--)
    {
        int x, a, b;
        cin >> x >> a >> b;

        if (x == 1)
        {
            // union
        }
        else
        {
            // find
        }
    }    

}